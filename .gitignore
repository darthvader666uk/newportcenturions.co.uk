_site/
*.gem

# ruby/setup-ruby installs gems here in CI (BUNDLE_PATH=vendor/bundle).
# Untracked files there make wrangler treat the workspace as dirty on deploy.
vendor/

# Jekyll specific
.jekyll-cache/
.jekyll-metadata

# Sass cache
.sass-cache/

# OS generated files
.DS_Store
Thumbs.db
*.Identifier
*.Zone.Identifier
server.log
.vscode
# Local AI Unit test setup
.claude
.codegpt
.agents
.cursor
.claude-config
.wellread
.ncp
.mcp
.github/copilot-instructions.md

.clinerules
.opencode
.windsurf
.config
/.vscode/CALIBER_LEARNINGS.md
.graymatter

# Python cache
__pycache__/
*.pyc

# Local secrets (the calendar's private iCal URL). This repo is public:
# never let these get committed.
.env
.env.*
!.env.example
